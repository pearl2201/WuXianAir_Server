{application, dbslave,
  [{description, "ZY database slave application"},
   {vsn, "1.0"},
   {modules, [dbapp, dbslave, dbsup,db_ini,db_tools]},
   {registered,[dbapp, dbslave, dbsup]},
   {applications, [kernel, stdlib]},
   {mod, {dbapp, [dbslave]}}
  ]
 }.
